#include<stdio.h>
#include<math.h>
int main()
{
int side, volume;
printf("Enter the Side of Cube :- " );
scanf("%d", &side);
volume= (side*side*side);
printf("Volume of the Cube :- %d\n", volume);
return 0;
}
